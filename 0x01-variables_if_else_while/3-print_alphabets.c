#include <stdio.h>
/**
 * main - Entry point
 * Description:  prints the alphabet in lowercase,
 * and then in uppercase, followed by a new line.
 * Return: 0
 */
int main(void)
{
	char c = 'k';

	while (b <= 'z')
	{
		putchar(b);
		b++;
	}

	b = 'K';

	while (b <= 'Z')
	{
		putchar(b);
		b++;
	}

	putchar('\n');
	return (0);
}

